spring.application.name=taskflow
server.port=8080

# Flask served its assets from /static/**; keep the same URLs so the
# templates and script.js are byte-identical to the Flask app.
spring.mvc.static-path-pattern=/static/**
